# Database Configuration
# -----------------------
# SQLite (default - simplest option, no external database needed):
# DATABASE_URL="file:/data/skylite.db"
#
# PostgreSQL (for production or multi-user deployments):
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/skylight"
#
# If DATABASE_URL is not set, SQLite is used by default at /data/skylite.db

# Prisma Schema Sync (SQLite only)
# --------------------------------
# Set to "true" to allow destructive schema changes during startup.
# WARNING: This may cause data loss if schema changes require dropping columns/tables.
# Only use this if you understand the risks.
# PRISMA_ACCEPT_DATA_LOSS=true

# Google Calendar Integration (Optional)
# Get these from Google Cloud Console: https://console.cloud.google.com/
# 1. Create a project
# 2. Enable Google Calendar API
# 3. Create OAuth 2.0 Client ID (Web application)
# 4. Set redirect URI to: http://localhost:3000/api/integrations/google_calendar/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
